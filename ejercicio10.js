const numbers = [12, 21, 38, 5, 45, 37, 6];
function average(numberList) {
    let average = 0;
    for(number of numberList){
        average+=number;
        
    }
    average=average/numberList.length;
    console.log(average);
    
}
average(numbers);